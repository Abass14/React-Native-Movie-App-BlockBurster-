import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

export const Splash = () =>{
    return(
        <Text>Splash Screen</Text>
    )
}

const styles = StyleSheet.create({
    
})